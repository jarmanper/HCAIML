# Optional conda mirror of requirements.txt. The pip route is still the
# blessed path; this file is here for graders who prefer conda.
name: finsent-compare
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - pip
  - pip:
      - torch>=2.2,<3.0
      - transformers>=4.40,<5.0
      - datasets>=2.18,<3.0
      - vaderSentiment>=3.3.2
      - nltk>=3.8
      - scikit-learn>=1.4
      - pandas>=2.1
      - numpy>=1.26
      - pyarrow>=14.0
      - matplotlib>=3.8
      - seaborn>=0.13
      - jupyter>=1.0
      - tqdm>=4.66
      - pytest>=8.0
